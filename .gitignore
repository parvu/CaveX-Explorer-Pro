node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
ros2_ws/install/
ros2_ws/log/
ros2_ws/cavex_ate_runs.csv
ros2_ws/eeprom.bin
ros2_ws/mav.parm
ros2_ws/mav.tlog
ros2_ws/mav.tlog.raw
ros2_ws/logs/
.local-deps/
__pycache__/
*.pyc
.worktrees/

# superpowers SDD scratch workspace (ledger, briefs, review packages)
.superpowers/

# Superpowers design docs / implementation plans (kept locally, not shipped)
docs/superpowers/

# PX4 SITL tree: cloned locally, deliberately NOT vendored into git history
# (README documents this; it was not actually ignored before -- a stray `git add -A`
# would have committed a multi-GB second autopilot tree).
PX4-Autopilot/

# Launch runbook: full historical notes, design rationale (kept locally, not shipped)
launch.txt
